//Write a program that sorts an array of integers using the Merge sort algorithm.

using System;

class MergeSort
{
    private int[] MergeSort( int[] merged)
    {
        if(merged.Length == 1)
        {
            return merged;
        }

    }
    static void Main()
    {
        int[] array = { 2, 3, 9, 5, 10, 7, 1 };
    }
}